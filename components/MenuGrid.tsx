'use client';
import { useState } from 'react'; import { img, menu } from '@/lib/data';
export default function MenuGrid({limit}:{limit?:number}) { const [filter,setFilter]=useState('All'); const [cart,setCart]=useState(0); const categories=['All','Coffee','Mains','Dessert','Drinks']; const items=menu.filter(x=>filter==='All'||x.category===filter).slice(0,limit); return <><div className="filter">{categories.map(x=><button key={x} className={filter===x?'active':''} onClick={()=>setFilter(x)}>{x}</button>)}</div><div className="menuGrid">{items.map((x,i)=><article className="dish" key={x.name}><div className="dishImage" style={{backgroundImage:`url(${img(x.image)})`}}>{x.tag&&<b>{x.tag}</b>}</div><div className="dishInfo"><div><h3>{x.name}</h3><p>{x.desc}</p></div><div className="dishBottom"><strong>₹{x.price}</strong><button onClick={()=>setCart(cart+1)} aria-label={'Add '+x.name}>+</button></div></div></article>)}</div>{cart>0&&<div className="cartToast">{cart} item{cart>1?'s':''} added to your order</div>}</> }
